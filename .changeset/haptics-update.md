---
"@kenos-ui/react-button": minor
"@kenos-ui/react": minor
---

Replaced `useHapticFeedback` with a new semantic `useHaptics` hook powered by `web-haptics`. This update introduces semantic intents (`trigger('save')` instead of `'light'`) and a global toggle capability (`setEnabled`).
